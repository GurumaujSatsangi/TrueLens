# TrueLens (scaffold)

This folder contains a scaffold for the TrueLens project.

Backend: `truelens/backend`

Quick start:

```powershell
cd truelens/backend
npm install
npm run dev
```

Then open `http://localhost:5000`.

Fill in `.env` using `.env.example` before starting.
