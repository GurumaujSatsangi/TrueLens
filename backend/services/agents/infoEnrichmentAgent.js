// TODO: Implement enrichment logic (third-party enrichment, heuristics)
export async function runInfoEnrichment(provider) {
  // Placeholder - enrich provider with external info
  return { enriched: false };
}
